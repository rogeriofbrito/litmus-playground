apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.users.app.name }}
  labels:
    {{- include "cnpg.labels" . | nindent 4 }}
type: kubernetes.io/basic-auth
stringData:
  username: "{{ .Values.users.app.secrets.username }}"
  password: "{{ .Values.users.app.secrets.password }}"
