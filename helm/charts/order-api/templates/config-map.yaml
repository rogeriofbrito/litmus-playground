apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "order-api.fullname" . }}
  labels:
    {{- include "order-api.labels" . | nindent 4 }}
data:
  {{- with .Values.configs }}
    {{- toYaml . | nindent 2 }}
  {{- end }}
